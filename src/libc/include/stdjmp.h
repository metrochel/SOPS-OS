//
//  stdjmp.h - Переходы между функциями
//
//  Осуществление переходов между функциями.
//  О как, это мой редактор сам вставил!
//

#include <etc/decl.h>

BEGIN_DECLS

// `jmp_buf` - структура для сохранения контекста исполнения.
typedef struct jmp_buf {
    void *stack;            // Указатель на стек
    void *frame;            // Указатель на фрейм
    void *return_address;   // Указатель на возвращаемый адрес
} jmp_buf;

// Сохраняет контекст исполнения в `env` и возвращает 0.
int setjmp(jmp_buf env);

// Восстанавливает контекст исполнения из `env` и возвращает `val`.
void longjmp(jmp_buf env, int val);

// Ну обалдеть! Мой редактор стал умным и написал всё за меня!

END_DECLS